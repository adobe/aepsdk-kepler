compressionLevel: mixed

enableGlobalCache: false

nodeLinker: node-modules

checksumBehavior: update

# amzn scope auth removed for public CI - use public registry only.
# If you need private amzn packages locally, add npmAuthToken via env or local .yarnrc.
